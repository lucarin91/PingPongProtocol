//number of peers
peers = 20;

//density of the connections number from 0 to 1
connection = 0.1;

//random send (the L at the end represent a 64-bit long number)
srand = 234234324443334L
//in a 32bit system use a shouter number as 48121642

//witch peer log and in witch way
logger =  {
  p2 = {stdout = true;},
  p10 = {stdout = true;},
  topology = {stdout = true;}
};
